---
interface Props {
  className?: string;
}

const { className = "" } = Astro.props;
---

<div class={`rounded-lg border border-arvi-text/10 bg-arvi-background p-4 shadow-sm ${className}`}>
  <slot />
</div>
